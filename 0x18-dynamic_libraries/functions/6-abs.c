#include "main.h"
/**
 *_abs - funtion computes absolute value of an integer
 *@n: integer to check
 *Return:absolute value of an integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
